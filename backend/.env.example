# Conexão PostgreSQL (obrigatória) — usar driver asyncpg
DATABASE_URL=postgresql+asyncpg://user:pass@host:5432/faturasus

# Origens permitidas para CORS (opcional — default inclui Railway + localhost)
# CORS_ORIGINS=["https://faturasus-production.up.railway.app","http://localhost:5173"]
